# Supply-chain hardening: fail installs when a dependency wants to execute an
# install-time lifecycle script that has not been explicitly reviewed and
# pinned in package.json#allowScripts.
strict-allow-scripts=true
