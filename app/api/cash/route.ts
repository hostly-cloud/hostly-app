import { NextResponse } from "next/server";
import { isAuthErrorResponse, requireAuthenticatedRestaurant } from "@/lib/server/auth/require-authenticated-restaurant";
import { addCashMovementV2, CashRegisterV2Error, closeCashDayV2, closeCashSessionV2, countCashBlindV2, getCashWorkspaceV2, openCashSessionV2, reopenCashCountV2 } from "@/lib/server/cash/cash-register-v2";
const jsonError=(status:number,error:string)=>NextResponse.json({ok:false,error},{status});
export async function GET(req:Request){try{const auth=await requireAuthenticatedRestaurant(req);if(isAuthErrorResponse(auth))return auth;const snapshot=await getCashWorkspaceV2({db:auth.db,restaurantId:auth.restaurantId,actorRole:auth.role});return NextResponse.json({ok:true,snapshot},{headers:{"Cache-Control":"private, no-store"}});}catch(error){if(error instanceof CashRegisterV2Error)return jsonError(error.httpStatus,error.code);console.error("[cash:get]",error);return jsonError(500,"CASH_WORKSPACE_FAILED");}}
export async function POST(req:Request){try{const auth=await requireAuthenticatedRestaurant(req);if(isAuthErrorResponse(auth))return auth;const body=await req.json().catch(()=>null) as Record<string,unknown>|null;if(!body)return jsonError(400,"INVALID_BODY");switch(body.action){case"session.open":return NextResponse.json({ok:true,sessionId:await openCashSessionV2({db:auth.db,restaurantId:auth.restaurantId,actorUid:auth.uid,actorEmail:auth.email,actorRole:auth.role,openingFloat:body.openingFloat})});case"movement.add":await addCashMovementV2({db:auth.db,restaurantId:auth.restaurantId,actorUid:auth.uid,actorEmail:auth.email,actorRole:auth.role,sessionId:body.sessionId,type:body.type,amount:body.amount,reason:body.reason});return NextResponse.json({ok:true});case"session.count":await countCashBlindV2({db:auth.db,restaurantId:auth.restaurantId,actorUid:auth.uid,actorEmail:auth.email,actorRole:auth.role,sessionId:body.sessionId,countedCash:body.countedCash});return NextResponse.json({ok:true});case"session.reopen":await reopenCashCountV2({db:auth.db,restaurantId:auth.restaurantId,actorUid:auth.uid,actorEmail:auth.email,actorRole:auth.role,sessionId:body.sessionId,reason:body.reason});return NextResponse.json({ok:true});case"session.close":return NextResponse.json({ok:true,result:await closeCashSessionV2({db:auth.db,restaurantId:auth.restaurantId,actorUid:auth.uid,actorEmail:auth.email,actorRole:auth.role,sessionId:body.sessionId,discrepancyReason:body.discrepancyReason})});case"day.close":return NextResponse.json({ok:true,result:await closeCashDayV2({db:auth.db,restaurantId:auth.restaurantId,actorUid:auth.uid,actorEmail:auth.email,actorRole:auth.role,note:body.note})});default:return jsonError(400,"UNKNOWN_CASH_OPERATION");}}catch(error){if(error instanceof CashRegisterV2Error)return jsonError(error.httpStatus,error.code);console.error("[cash:post]",error);return jsonError(500,"CASH_OPERATION_FAILED");}}
